CREATE DATABASE `amazur` COLLATE 'utf8_unicode_ci';

CREATE TABLE items (
`id` INT(10) NOT NULL UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT 'Primary key',
`item` VARCHAR(32) NOT NULL COMMENT 'Items list'
}